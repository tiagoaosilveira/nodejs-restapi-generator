const {{ name }}Service = require('../services/{{ name }}');

exports.listAll{{ ups name }} = async (req, res) => {
    try {
        const data = await {{ name }}Service.findAll{{ ups name }}();
        res.status(200).send(data);
    } catch (e) {
        res.status(500).send({message: 'Failed to get {{ ups name }}'});
    }
};

exports.find{{ up name }}ById = async (req, res) => {
    try {
        const data = await {{ name }}Service.find{{ up name }}ById(req.params.id);
        if (data) {
            res.status(200).send(data);
        } else {
            res.status(200).send({message: '{{ up name }} not found'})
        }
    } catch (e) {
        res.status(500).send({message: 'Failed to get {{ up name }} By Id'});
    }
};

exports.insert{{ up name }} = async (req, res) => {
    if(!req.body.name || !req.body.quantity || !req.body.price) {
        res.status(422).send({message: 'Missing {{ up name }} fields'});
    }
    try {
        const data = await {{ name }}Service.insert{{ up name }}(req.body);
        res.status(201).send(data);
    } catch (e) {
        res.status(500).send({message: 'Failed to insert {{ up name }}'});
    }
}

exports.update{{ up name }} = async (req, res) => {
    if(!req.body.name || !req.body.quantity || !req.body.price) {
        res.status(422).send({message: 'Missing {{ up name }} fields'});
    }
    try {
        const data = await {{ name }}Service.update{{ up name }}(req.params.id, req.body);
        if (data.rowsUpdated) {
            res.status(200).send(data.updatedBook);
        } else {
            res.status(404).send({message: '{{ up name }} not found, nothing to update'});
        }
        
    } catch (e) {
        res.status(500).send({message: 'Failed to update {{ up name }}'});
    }
}

exports.delete{{ up name }} = async (req, res) => {
    try {
        const data = await {{ name }}Service.delete{{ up name}}(req.params.id);
        if (data) {
            res.status(200).send({
                message: '{{ up name }} succesfully removed!'
            });
        } else {
            res.status(404).send({message: '{{ up name }} not found, nothing to remove'});
        }
    } catch (e) {
        res.status(500).send({message: 'Failed to remove {{ up name }}'});
    }
}